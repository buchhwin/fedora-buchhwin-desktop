// The desktop itself. Filled in from M3 onwards (bar), M4 (notch).
// Kept as a real file from day one so the tool/UI split in shell.qml is
// exercised by every headless run, not only once the UI exists.
import QtQuick
import Quickshell

Scope {
}
